version: "3.7"
services:
    webcrawler:
        container_name: web-scraper
        restart: unless-stopped
        ports:
            - 5000:5000
        volumes:
            - ~/.aws:/root/.aws:ro
        build:
            context: .
            dockerfile: Dockerfile
